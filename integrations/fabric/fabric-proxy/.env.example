# Fabric Proxy Configuration
# ===========================
# This service uses Claude Agent SDK with OAuth authentication
# No Anthropic API key required - uses your Max subscription!

# API Security
# Generate a secure key: openssl rand -hex 32
FABRIC_API_KEY=your-secure-api-key-here

# Note: Claude credentials are mounted from ~/.claude/
# Run 'claude setup-token' on your machine first to create them
